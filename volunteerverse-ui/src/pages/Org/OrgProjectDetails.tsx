import { createStyles } from "@mantine/styles";

const useStyles = createStyles((theme) => ({

}));

function OrgProjectDetails() {
    const { classes } = useStyles()
    return (
        <div>OrgProjectDetails</div>
    )
}

export default OrgProjectDetails